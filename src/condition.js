import {getOperators, registerOperator, unregisterOperator} from './operator';
import {getMutators, prefix, postfix, registerMutator, unregisterMutator} from './mutator';

function composeCondition(expr) {

}

export {
    composeCondition
}